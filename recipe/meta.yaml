{% set name = "sophios" %}
{% set version = "0.4.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/PolusAI/sophios/archive/refs/tags/v{{ version }}.tar.gz
  sha256: cafead1cf0da4c97d149bc11b97d834a1c67362c77de6ff515b111e512224041

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools >=42
    - pip
    - wheel
    - versioneer ==0.29
    - tomli
  run:
    - python >={{ python_min }}
    - python-graphviz
    - jsonschema
    - cwltool
    - toil
    - pyyaml
    - requests
    - mergedeep
    - networkx
    - cwl-utils >=0.32
    - typeguard
    - pydantic >=2.6
    - pydantic-settings
    - ruamel.yaml.clib
    - fastapi
    - docker-py
    - podman-py
    - python-jose
    - uvicorn
    - referencing
    - aiofiles

test:
  imports:
    - sophios
  commands:
    - pip list
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://pypi.org/project/sophios/
  summary: DSL for inferring the edges of a CWL workflow DAG
  dev_url: https://github.com/PolusAI/sophios
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - sameeul
